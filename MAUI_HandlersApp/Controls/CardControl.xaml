<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUI_HandlersApp.Controls.CardControl">
    <!--Step 1 for a Very Simple Custom Control-->
    <!--<VerticalStackLayout>-->
        <!--<Label x:Name="TitleLabel"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />-->
    <!--</VerticalStackLayout>-->
    
    <!--Step 2 a custom control with Control Template-->
    <ContentView.ControlTemplate>
        <ControlTemplate>
            <Frame>
                <VerticalStackLayout> 
                    <Label Text="{TemplateBinding Title}"/>
                    <!--Provide a facility to this Control to Contain Other UI Elements in Visual Tree-->
                    <ContentPresenter/>
                </VerticalStackLayout>
            </Frame>
        </ControlTemplate>
    </ContentView.ControlTemplate>
    
</ContentView>
